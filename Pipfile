[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "*" # Django
django-extensions = "*" # for shell_plus and other useful commands
django-storages = "*" # for S3
django-environ = "*" # for environment variables
redis = "*" # in-memory cache
django-redis = "*" # for Redis cache
celery = "*" # for background tasks
django-celery-beat = "*" # for periodic tasks
django-tailwind = "*" # for tailwind
psycopg2 = "*" # for postgres

[dev-packages]
django-debug-toolbar = "*" # for debugging
psycopg2-binary = "*" # for postgres on local
werkzeug = "*" # for runserver_plus command

[requires]
python_version = "3.9"
python_full_version = "3.9.15"
